# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
sevensegment
.vscode
.idea

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Don't track the actual config files
config.yaml
config.yml